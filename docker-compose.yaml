version: "3.10"
services:
  eatiplan-user-service:
    container_name: eatiplan-user-service
    restart: always
    env_file: .env
    build: .
    ports:
      - $GRPC_PORT:9080
    networks:
      - user-service

networks:
  user-service:
    external: true
